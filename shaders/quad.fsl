#version 330 core
in vec2 TexCoord;
out vec4 fragColor;

uniform sampler2D texture1;

void main()
{
    vec4 fc = texture(texture1, TexCoord);

    if(fc.a < 0.1)
    {
        discard;
    }
    fragColor = fc;
}
